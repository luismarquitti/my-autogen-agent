# Project Plan

This document outlines the plan for setting up the `autogen-py-projeto` repository.

## Plan

1.  **Setup project environment.** (DONE)
    -   Create a Python virtual environment named `venv`.
2.  **Install dependencies.** (DONE)
    -   Activate the virtual environment.
    -   Install `pyautogen`, `python-dotenv`, and `pymarkdown-linter[all]`.
3.  **Configure the environment and editor.** (DONE)
    -   Create the `.env` file with a placeholder for the API key.
    -   Create the `.vscode` directory and the `settings.json` file inside it.
4.  **Create the application files.** (DONE)
    -   Create the `src` directory and the `main.py` file inside it.
    -   Create the `input.md` file.
5.  **Create documentation files.** (IN PROGRESS)
    -   Create `AGENTS.MD`.
    -   Create `GEMINI.MD`.
    -   Create `OPENAI.MD`.
    -   Create `COPILOT-INSTRUCTIONS.MD`.
    -   Create `PLANS.md` to document the plan and its status.
6.  **Complete pre commit steps**
    - Complete pre commit steps to make sure proper testing, verifications, reviews and reflections are done.
7.  **Submit the change.**
    -   Once all files are created, I will submit the change with a descriptive commit message.
